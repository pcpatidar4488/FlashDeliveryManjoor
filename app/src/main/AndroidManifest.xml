<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.manjooralam.flashdelivery">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA" />

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.HomeActivity" />
        <activity
            android:name=".activities.OrderOnPhoneActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.Contact_us" />
        <activity android:name=".activities.MyWalletActivity" />
        <activity android:name=".activities.AddAdressActivity" />
        <activity android:name=".activities.AboutUsActivity" />
        <activity android:name=".activities.FeaturedProductActivity" />
        <!-- <activity android:name=".activities.AboutUsActivity$LoginActivity" /> -->
        <activity android:name=".activities.SignUpActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.ForgotPasswordActivity" />
        <activity android:name=".activities.ResetPasswordActivty" />
        <activity android:name=".activities.OtpActivity" />
        <activity android:name=".activities.FruitsAndVegitableActivity" />
        <activity
            android:name=".activities.SearchActivity"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity android:name=".activities.OrderHistoryActivity" />
        <activity android:name=".activities.ShoppingActivity" />
        <activity android:name=".activities.NotificationActivity" />
        <activity android:name=".activities.OffersActivity" />
        <activity android:name=".activities.CakesAndFlowersActivity" />
        <activity android:name=".activities.GroceryAndStaplesActivity" />
        <activity android:name=".activities.HouseholdActivity" />
        <activity android:name=".activities.BeaverageActivity" />
        <activity android:name=".activities.PersonalCareActivity" />
        <activity android:name=".activities.DairyAndBreadActivity" />
        <activity android:name=".activities.BrandedFoodsActivity" />
        <activity android:name=".activities.AdminPanelActivity" />
        <activity android:name=".activities.SelectShoppingListActivity"/>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="87e18c706a5b2f0ed278ccced4a25dd53fc3fd39"
            />

        <activity
            android:name=".activities.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".activities.AdminPanelActivity" />
        <activity android:name=".activities.AdressBookActivity"></activity>

        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/>
    </application>

</manifest>